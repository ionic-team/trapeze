const config = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-configure-test',
  webDir: 'build',
  bundledWebRuntime: false,
  ios: {
    path: 'ios'
  },
  android: {
    path: 'android'
  }
};

export default config;
