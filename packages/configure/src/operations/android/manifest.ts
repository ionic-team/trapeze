import { Context } from '../../ctx';
import { Operation } from '../../definitions';

export default async function execute(_ctx: Context, op: Operation) {
  // const android = ctx.project.android;

  console.log(op);
}